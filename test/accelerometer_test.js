/*jshint jasmine: true */
"use strict";

function Accelerometer(context) {
  // context.navigator.userAgent.match
  var flip = true; // false

  var o =  Object.create({}, {
    start: {
      get: function (arg) {

      }
    }
  });

  o.start = function () {
    window.
  }
}
